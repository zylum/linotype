# Release 0.6.2 — Casablanca

## Summary
- Codename locked: **Casablanca** (iconic movie, previously unused)
- Focus areas: agent efficiency, domain discipline, release-note automation, CLI modularisation, CI re-enable

## Highlights
- Introduced per-version release notes (`docs/work/releases/<version>.md`) with movie naming enforcement
- Added `docs/domain/index.md` and domain-delta hooks in galley templates to stop monolith docs
- Refactored `cli/linotype` into modular commands plus new `release` helpers and bats tests
- Restored GitHub Actions (shellcheck, bats, domain lint)

## Galley / CLI changes
- `cli/linotype release init|note` manage version files and prevent duplicate movie names
- Galley templates now include a **Domain updates** checklist and release-note prompt
- `cli/check-domain.sh` (and CI) ensure domain docs exist and that galleys acknowledge doc changes

## Domain updates
- New `docs/domain/index.md` placeholder plus guidance on splitting modules
- Added CI lint + agent rules forcing teams to update domain docs when galleys ship

## Verification
- Shellcheck + bats tests run via GitHub Actions
- Manual smoke: `cli/linotype release init 0.6.2 Casablanca` (no duplicate warning), `cli/linotype release note 0.6.2 "Token-aware snapshots"`
